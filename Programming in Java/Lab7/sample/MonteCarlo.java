package sample;


/*
double Monte_Carlo(double xp, double xk, int n)
{
	double odleglosc = abs(xk - xp) / n;
	double f_srednie = 0;
	double f = 0;
	double *I = new double[n];

	for (int i = 0; i<n; i++)
	{
		for (int j = 0; j < n; j++)
		{
			double random = xp + i*odleglosc + static_cast <double>(rand()) / (RAND_MAX / ((xp + i*odleglosc + odleglosc - (xp + i*odleglosc))));
			I[i] = f_srednie + funkcja(random) / (n);
		}
		f_srednie = 0;
		f = f + I[i];


	}
	double calka = f*abs(xk - xp);
	return calka;
}
*/




public class MonteCarlo {

    //public double calk(double xp,double)



}
